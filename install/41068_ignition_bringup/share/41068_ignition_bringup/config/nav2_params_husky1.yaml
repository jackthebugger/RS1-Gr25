# NOTE ON AMCL
# This block is INERT. Nav2 is started through nav2_bringup/navigation_launch.py
# (see 41068_navigation.launch.py), which brings up only controller_server,
# smoother_server, planner_server, behavior_server, bt_navigator,
# waypoint_follower and velocity_smoother. AMCL and map_server live in
# nav2_bringup/localization_launch.py, which this package never includes, so no
# AMCL node ever runs and there is no competing publisher of husky1_map ->
# husky1_odom. SLAM Toolbox is the sole owner of that transform.
# tf_broadcast is nevertheless set to false so that if anyone later adds
# localization_launch.py while SLAM is running, the TF tree stays single-owner.
amcl:
  ros__parameters:
    use_sim_time: true
    tf_broadcast: false
    alpha1: 0.2
    alpha2: 0.2
    alpha3: 0.2
    alpha4: 0.2
    alpha5: 0.2
    base_frame_id: husky1_base_link
    beam_skip_distance: 0.5
    beam_skip_error_threshold: 0.9
    beam_skip_threshold: 0.3
    do_beamskip: false
    global_frame_id: husky1_map
    lambda_short: 0.1
    laser_likelihood_max_dist: 2.0
    laser_max_range: 100.0
    laser_min_range: -1.0
    laser_model_type: likelihood_field
    max_beams: 60
    max_particles: 2000
    min_particles: 500
    odom_frame_id: husky1_odom
    pf_err: 0.05
    pf_z: 0.99
    recovery_alpha_fast: 0.0
    recovery_alpha_slow: 0.0
    resample_interval: 1
    robot_model_type: nav2_amcl::DifferentialMotionModel
    save_pose_rate: 0.5
    sigma_hit: 0.2
    transform_tolerance: 1.0
    update_min_a: 0.2
    update_min_d: 0.25
    z_hit: 0.5
    z_max: 0.05
    z_rand: 0.5
    z_short: 0.05
    scan_topic: scan
bt_navigator:
  ros__parameters:
    use_sim_time: true
    global_frame: husky1_map
    robot_base_frame: husky1_base_link
    odom_topic: odom
    bt_loop_duration: 10
    default_server_timeout: 20
    wait_for_service_timeout: 1000
    plugin_lib_names:
    - nav2_compute_path_to_pose_action_bt_node
    - nav2_compute_path_through_poses_action_bt_node
    - nav2_smooth_path_action_bt_node
    - nav2_follow_path_action_bt_node
    - nav2_spin_action_bt_node
    - nav2_wait_action_bt_node
    - nav2_assisted_teleop_action_bt_node
    - nav2_back_up_action_bt_node
    - nav2_drive_on_heading_bt_node
    - nav2_clear_costmap_service_bt_node
    - nav2_is_stuck_condition_bt_node
    - nav2_goal_reached_condition_bt_node
    - nav2_goal_updated_condition_bt_node
    - nav2_globally_updated_goal_condition_bt_node
    - nav2_is_path_valid_condition_bt_node
    - nav2_initial_pose_received_condition_bt_node
    - nav2_reinitialize_global_localization_service_bt_node
    - nav2_rate_controller_bt_node
    - nav2_distance_controller_bt_node
    - nav2_speed_controller_bt_node
    - nav2_truncate_path_action_bt_node
    - nav2_truncate_path_local_action_bt_node
    - nav2_goal_updater_node_bt_node
    - nav2_recovery_node_bt_node
    - nav2_pipeline_sequence_bt_node
    - nav2_round_robin_node_bt_node
    - nav2_transform_available_condition_bt_node
    - nav2_time_expired_condition_bt_node
    - nav2_path_expiring_timer_condition
    - nav2_distance_traveled_condition_bt_node
    - nav2_single_trigger_bt_node
    - nav2_goal_updated_controller_bt_node
    - nav2_is_battery_low_condition_bt_node
    - nav2_navigate_through_poses_action_bt_node
    - nav2_navigate_to_pose_action_bt_node
    - nav2_remove_passed_goals_action_bt_node
    - nav2_planner_selector_bt_node
    - nav2_controller_selector_bt_node
    - nav2_goal_checker_selector_bt_node
    - nav2_controller_cancel_bt_node
    - nav2_path_longer_on_approach_bt_node
    - nav2_wait_cancel_bt_node
    - nav2_spin_cancel_bt_node
    - nav2_back_up_cancel_bt_node
    - nav2_assisted_teleop_cancel_bt_node
    - nav2_drive_on_heading_cancel_bt_node
    - nav2_is_battery_charging_condition_bt_node
bt_navigator_navigate_through_poses_rclcpp_node:
  ros__parameters:
    use_sim_time: true
bt_navigator_navigate_to_pose_rclcpp_node:
  ros__parameters:
    use_sim_time: true
controller_server:
  ros__parameters:
    use_sim_time: true
    # 10 Hz control matches the 10 Hz lidar and the 10 Hz local costmap, so a
    # newly detected obstacle can influence the very next velocity command.
    controller_frequency: 10.0
    min_x_velocity_threshold: 0.001
    min_y_velocity_threshold: 0.001
    min_theta_velocity_threshold: 0.001
    failure_tolerance: 0.3
    progress_checker_plugin: progress_checker
    goal_checker_plugins:
    - general_goal_checker
    controller_plugins:
    - FollowPath
    progress_checker:
      plugin: nav2_controller::SimpleProgressChecker
      # Must move 0.5 m within 20 s or the behaviour tree runs recovery. The
      # allowance is generous because the robot legitimately pauses while
      # rotating on the spot to face a freshly replanned path.
      required_movement_radius: 0.3
      movement_time_allowance: 30.0
    general_goal_checker:
      stateful: true
      plugin: nav2_controller::SimpleGoalChecker
      # Tight enough that "goal reached" means the robot is actually at the
      # goal. The original 1.0 m / 90 deg tolerance let it stop a full robot
      # length away and still report success.
      xy_goal_tolerance: 0.25
      yaw_goal_tolerance: 0.35
    FollowPath:
      # DWB was tried first and failed the dynamic-obstacle test: once a wall
      # appeared on the path it sat in front of the inflation and the progress
      # checker aborted ("Failed to make progress") instead of tracking the
      # new global plan around the ends. Regulated Pure Pursuit follows the
      # NavFn path and uses the local costmap only to slow/stop, which is the
      # right split for this stack (global planner owns detours).
      plugin: nav2_regulated_pure_pursuit_controller::RegulatedPurePursuitController
      desired_linear_vel: 0.5
      lookahead_dist: 0.6
      min_lookahead_dist: 0.3
      max_lookahead_dist: 0.9
      lookahead_time: 1.5
      rotate_to_heading_angular_vel: 1.2
      transform_tolerance: 0.5
      use_velocity_scaled_lookahead_dist: true
      min_approach_linear_velocity: 0.05
      approach_velocity_scaling_dist: 0.8
      use_collision_detection: true
      max_allowed_time_to_collision_up_to_carrot: 1.0
      use_regulated_linear_velocity_scaling: true
      use_cost_regulated_linear_velocity_scaling: true
      cost_scaling_dist: 0.6
      cost_scaling_gain: 1.0
      inflation_cost_scaling_factor: 3.0
      regulated_linear_scaling_min_radius: 0.9
      regulated_linear_scaling_min_speed: 0.2
      use_rotate_to_heading: true
      rotate_to_heading_min_angle: 0.785
      max_angular_accel: 3.2
      max_robot_pose_search_dist: 10.0
# FOOTPRINT AND INFLATION
# The Husky chassis is 0.987 x 0.571 m and the wheels widen it to about 0.685 m
# (wheel centres at y = +-0.2854, wheel width 0.1143). The original 1.0 x 1.0 m
# square footprint was therefore 0.3 m too wide, and the 2.0 m inflation radius
# put a high-cost halo more than three robot widths across every tree -- in a
# 15 x 15 m world that leaves almost no low-cost space to plan through.
# Footprint below is the real hull plus a small margin; inflation gives roughly
# 0.37 m of clearance beyond the robot's side.
local_costmap:
  local_costmap:
    ros__parameters:
      # Matches controller_frequency so the controller always sees fresh cells.
      update_frequency: 10.0
      publish_frequency: 5.0
      global_frame: husky1_odom
      robot_base_frame: husky1_base_link
      use_sim_time: true
      rolling_window: true
      width: 10
      height: 10
      resolution: 0.05
      footprint: '[ [0.55, 0.38], [0.55, -0.38], [-0.55, -0.38], [-0.55, 0.38] ]'
      plugins:
      - obstacle_layer
      - inflation_layer
      inflation_layer:
        plugin: nav2_costmap_2d::InflationLayer
        cost_scaling_factor: 3.0
        inflation_radius: 0.75
      obstacle_layer:
        plugin: nav2_costmap_2d::ObstacleLayer
        enabled: true
        observation_sources: scan
        scan:
          topic: scan
          # The lidar sits 0.845 m above the ground, so a scan return is only
          # ever at that height. The band below accepts it and rejects nothing
          # useful; raising min_obstacle_height above 0.845 would silently
          # discard every obstacle.
          min_obstacle_height: 0.0
          max_obstacle_height: 2.0
          clearing: true
          marking: true
          data_type: LaserScan
          # Raytracing out to 20 m clears stale cells across the whole rolling
          # window; marking is limited to 12 m where the scan is most reliable.
          raytrace_max_range: 20.0
          raytrace_min_range: 0.0
          obstacle_max_range: 12.0
          obstacle_min_range: 0.0
          inf_is_valid: false
      always_send_full_costmap: true
global_costmap:
  global_costmap:
    ros__parameters:
      # 5 Hz is enough for the global costmap: the behaviour tree only replans
      # at 1 Hz, so updating faster would burn CPU without changing behaviour.
      update_frequency: 5.0
      publish_frequency: 2.0
      global_frame: husky1_map
      robot_base_frame: husky1_base_link
      use_sim_time: true
      footprint: '[ [0.55, 0.38], [0.55, -0.38], [-0.55, -0.38], [-0.55, 0.38] ]'
      resolution: 0.1
      # SLAM Toolbox only grows the occupancy grid to the bounding box of
      # returns that actually hit something. In an open yard that box is a few
      # metres around the nearest tree, so a goal in clear space (e.g. 5 m
      # south of the origin) is literally off the map and NavFn aborts with
      # "goal sent to the planner is off the global costmap".
      # allow_unknown only helps for unknown *cells*; it cannot invent cells
      # that are not on the grid. A rolling window sized for both worlds
      # (simple_trees ~15 m, large_demo ~25 m) keeps every reasonable goal on
      # the costmap; the lidar obstacle layer fills in what the robot can see.
      rolling_window: true
      width: 40
      height: 40
      # SLAM starts with an empty map, so unknown space must be tracked and the
      # planner must be allowed to route through it (planner_server below).
      track_unknown_space: true
      plugins:
      - static_layer
      - obstacle_layer
      - inflation_layer
      obstacle_layer:
        plugin: nav2_costmap_2d::ObstacleLayer
        enabled: true
        observation_sources: scan
        scan:
          topic: scan
          min_obstacle_height: 0.0
          max_obstacle_height: 2.0
          clearing: true
          marking: true
          data_type: LaserScan
          raytrace_max_range: 20.0
          raytrace_min_range: 0.0
          obstacle_max_range: 12.0
          obstacle_min_range: 0.0
          inf_is_valid: false
      static_layer:
        plugin: nav2_costmap_2d::StaticLayer
        map_subscribe_transient_local: true
      inflation_layer:
        plugin: nav2_costmap_2d::InflationLayer
        cost_scaling_factor: 3.0
        inflation_radius: 0.75
      always_send_full_costmap: true
map_server:
  ros__parameters:
    use_sim_time: true
    yaml_filename: ''
map_saver:
  ros__parameters:
    use_sim_time: true
    save_map_timeout: 5.0
    free_thresh_default: 0.25
    occupied_thresh_default: 0.65
    map_subscribe_transient_local: true
planner_server:
  ros__parameters:
    # The default behaviour tree replans at 1 Hz, so 5 Hz is ample and stops
    # Nav2 logging "planner missed its rate" warnings.
    expected_planner_frequency: 5.0
    use_sim_time: true
    planner_plugins:
    - GridBased
    GridBased:
      plugin: nav2_navfn_planner/NavfnPlanner
      tolerance: 0.5
      # A* expands far fewer cells than the full Dijkstra wavefront, which
      # matters because this planner now runs every second for the whole
      # mission. Path quality is equivalent on this grid.
      use_astar: true
      # Required: SLAM builds the map as the robot drives, so the goal is
      # usually in unknown space when the mission starts.
      allow_unknown: true
smoother_server:
  ros__parameters:
    use_sim_time: true
    smoother_plugins:
    - simple_smoother
    simple_smoother:
      plugin: nav2_smoother::SimpleSmoother
      tolerance: 1.0e-10
      max_its: 1000
      do_refinement: true
behavior_server:
  ros__parameters:
    costmap_topic: local_costmap/costmap_raw
    footprint_topic: local_costmap/published_footprint
    cycle_frequency: 10.0
    behavior_plugins:
    - spin
    - backup
    - drive_on_heading
    - assisted_teleop
    - wait
    spin:
      plugin: nav2_behaviors/Spin
    backup:
      plugin: nav2_behaviors/BackUp
    drive_on_heading:
      plugin: nav2_behaviors/DriveOnHeading
    wait:
      plugin: nav2_behaviors/Wait
    assisted_teleop:
      plugin: nav2_behaviors/AssistedTeleop
    global_frame: husky1_odom
    robot_base_frame: husky1_base_link
    transform_tolerance: 0.1
    use_sim_time: true
    simulate_ahead_time: 2.0
    max_rotational_vel: 1.0
    min_rotational_vel: 0.4
    rotational_acc_lim: 3.2
robot_state_publisher:
  ros__parameters:
    use_sim_time: true
waypoint_follower:
  ros__parameters:
    use_sim_time: true
    loop_rate: 20
    stop_on_failure: false
    waypoint_task_executor_plugin: wait_at_waypoint
    wait_at_waypoint:
      plugin: nav2_waypoint_follower::WaitAtWaypoint
      enabled: true
      waypoint_pause_duration: 200
velocity_smoother:
  ros__parameters:
    use_sim_time: true
    smoothing_frequency: 20.0
    scale_velocities: false
    feedback: OPEN_LOOP
    # Kept in step with the DWB limits above so the smoother never becomes the
    # binding constraint (or silently allows more than the controller planned).
    max_velocity:
    - 0.6
    - 0.0
    - 1.2
    min_velocity:
    - -0.6
    - 0.0
    - -1.2
    max_accel:
    - 2.5
    - 0.0
    - 3.2
    max_decel:
    - -2.5
    - 0.0
    - -3.2
    odom_topic: odom
    odom_duration: 0.1
    deadband_velocity:
    - 0.0
    - 0.0
    - 0.0
    velocity_timeout: 1.0
