from nav2_simple_commander.robot_navigator import BasicNavigator
import rclpy
> From your code, you can interact with Nav2 using UP
“actions” nav = BasicNavigator()
¢ Actions are like topics, but have a request and response, And aay. settnitisirose(init pose)
goals and results: nav.waituntilnav2active() # if autostarted, else use Lifecyclestartup()
https://docs.ros.org/en/foxy/Tutorials/Beginner-CLI-Tools/Understanding-ROS2- Bae
Actions/Understanding-ROS2-Actions.html <
path = nav.getPath(init_pose, goal_pose)
. smoothed_path = nav.smoothPath(path)
« 41068 package provides example code
> The “Commander API” simplifies how to call and track nav.goToPose(goal_pose)
. while not nav.isTaskComplete():
feedback = nav.getFeedback()
the actions
. if feedback.navigation_duration > 600:
Overview: https://docs.nav2.org/commander_api/index.html nav-concelTask()
eo
> Code with examples: Rese oe eect)
https://github.com/ros-navigation/navigation2/tree/main/nav2_simple_commander if result == TaskResult. SUCCEEDED:
int( ‘Goal succeeded!"
eee ae ae
print(‘Goal was canceled!*)
elif result == TaskResult.FAILED:
print(‘Goal failed!")
66