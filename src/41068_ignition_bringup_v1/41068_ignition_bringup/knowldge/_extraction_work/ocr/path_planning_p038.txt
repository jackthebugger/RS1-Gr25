A* (“A star’) Algorithm
> Analgorithm for the general shortest path problem that is faster than Dijkstra’s

algorithm in typical scenarios
> Relies on a user-defined heuristic estimate
> One change to the algorithm: Define the node costs f, g, and h as:

xyh=QGgxyt+tha«
Estimated path cost from — f ( ) gl ) ( )
start node to x to goal Yt \
Path-cost: cost of the shortest Heuristic: Estimate of the
path from start node to x distance to the goal
> Heuristic must be admissible (not overestimate) and consistent (monotone increasing)
> Equivalent to Dijkstra’s algorithm when h(x) = 0
38